version: '3.9'

services:
    app:
        container_name: nextcloud-app
        image: nextcloud
        restart: always
        ports:
            - 8080:80
        volumes:
            - nextcloud:/var/www/html
        env_file: ../stack.env
        networks:
            - proxy_network

volumes:
    nextcloud:

networks:
    proxy_network:
        external: true
